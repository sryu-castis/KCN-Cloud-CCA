<div id="popup_large01"> <!--팝업 전체 시작 -->
    <div class="bg_top"> <!--배경 Top 영역 시작 -->
        <div class="title">
            <span class="text"><%= model.getPossessionCoupon().getEventName() %></span>
        </div>
        <div class="tit_line"></div><!--20150622 추가 -->
<%
		var eventType = model.getPossessionCoupon().getEventType(); // eventType=="002" || eventType=="304"
		if(eventType == "002")	{
			var headText = "VOD 구매가 취소되었습니다."; 
			var eventTitle = "취소 쿠폰";
			var eventName = model.getPossessionCoupon().getContentPurchased();
		}
		else	{
			var headText = "쿠폰 발급이 취소되었습니다.";
			var eventTitle = "상품명";
			var eventName = model.getPossessionCoupon().getCouponName();
		}
        
        var price = UIHelper.addThousandSeparatorCommas(model.getPossessionCoupon().getEventMoney());
%>
        <div class="body_title"><%= headText%></div>
        <div class="headline_bg01">
          <span class="subtext03">
                <p class="row">
                    <span class="head_text"><%= eventTitle %></span>
                    <span class="sub_text01"><%= eventName %></span>
                </p>
                <p class="row">
                  <span class="head_text">취소금액</span>
                  <span class="sub_text02">
                      <span class="num"><%= price %></span>
                      <span class="kor">원</span>
                  </span>
                </p>
		  </span>           
        </div>
        <div class="bodyline_aa">자세한 내용은 콜센터로 문의 부탁드립니다.</div>
        <div class="area_btn">
            <span class="bt_147 focus">확인</span> <!--unfocus or focus 중 택일-->   
        </div>
    </div>
</div><!--//팝업 전체 끝-->