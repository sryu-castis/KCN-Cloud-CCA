<div id="popup_large03"> <!--팝업 전체 시작 -->
    <div class="bg_top"> <!--배경 Top 영역 시작 -->
        <div class="title">
            <span class="text"><%= model.getPossessionCoupon().getEventName() %></span>
        </div>
        <div class="tit_line"></div><!--20150622 추가 -->
 <%
		if(eventType=="104" || eventType=="201")	{
			
		}
		else	{
		//	return;
		}
        var headText = model.getPossessionCoupon().getCouponName();
        var eventType = model.getPossessionCoupon().getEventType();
        var price = UIHelper.addThousandSeparatorCommas(model.getPossessionCoupon().getEventMoney());
		var expireDate = model.getPossessionCoupon().getCouponExpirationDate();
		var eventDate = model.getPossessionCoupon().getEventDate();
		
%>
        <div class="body_title"><%= headText%></div>
        <div class="body_subtitle">유효기간이 경과되어 포인트가 소멸되었습니다.</div>
        <div class="headline_bg01">
            <span class="subtext01">
                <p class="row">
                    <span class="head_text">소멸금액</span>
                    <span class="sub_text"><%= price %>원</span>
                </p>
                <p class="row">
                    <span class="head_text">유효기간</span>
                    <span class="sub_text"><%= expireDate %></span>
                </p>                
			</span>
            <span class="subtext_line"></span>
            <span class="subtext02">
                <p class="row">
                    <span class="head_text">소멸날짜</span>
                    <span class="sub_text"><%= eventDate %></span>
                </p>
			</span>           
        </div>
<%
	var substring = "소멸된 포인트는 복구되지 않습니다.";
	if(eventType == "104")	{
		substring = "자세한 내용은 콜센터로 문의 부탁드립니다.";
	}
%>        
        <div class="bodyline_aa"><%=substring%></div>
        <div class="area_btn">
            <span class="bt_147 focus">확인</span> <!--unfocus or focus 중 택일-->   
        </div>
    </div><!--//배경 Top 영역 끝 -->
</div><!--//팝업 전체 끝-->